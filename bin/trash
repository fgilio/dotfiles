#!/usr/bin/env bash
# Move files to Trash instead of permanently deleting
# Cross-volume: files on external drives are permanently deleted (real rm)
# Supports: -f (ignore missing), -v (verbose)
set -euo pipefail

REAL_RM="/bin/rm"
trash_dir="${TRASH_DIR:-$HOME/.Trash}"

# Debug mode: RM_DEBUG=1 trash file
[[ "${RM_DEBUG:-0}" == "1" ]] && set -x

get_device() {
  stat -f '%d' "$1" 2>/dev/null || echo ""
}

# Shared with bin/rm so the two safety checks can't drift apart
# shellcheck source=bin/lib/dangerous-path.sh
source "${0%/*}/lib/dangerous-path.sh"

force=0
verbose=0

while getopts ":fv" opt; do
  case "$opt" in
    f) force=1 ;;
    v) verbose=1 ;;
    \?)
      echo "trash: illegal option -- $OPTARG" >&2
      exit 2
      ;;
  esac
done
shift $((OPTIND - 1))

[[ $# -eq 0 ]] && {
  echo "trash: missing operand" >&2
  exit 1
}

[[ -d "$trash_dir" ]] || mkdir -p -- "$trash_dir"
status=0
# Timestamp for deconflict names, computed only when a collision happens
ts=""
counter=0

trash_device="$(get_device "$trash_dir")"

# Pass 1: validate arguments. Survivors get their device looked up in ONE
# batched stat below — a stat fork per file made 200-file deletes pay ~0.5s
# in stat alone (fork+exec is the dominant per-file cost on macOS).
survivors=()
for src in "$@"; do
  # Dangerous-path check before existence so `trash -f ''` fails loudly like
  # `rm -f ''` does; -f must only forgive missing files
  if is_dangerous_path "$src"; then
    echo "trash: refusing dangerous path: '$src'" >&2
    status=1
    continue
  fi

  if [[ ! -e "$src" && ! -L "$src" ]]; then
    [[ $force -eq 1 ]] && continue
    echo "trash: $src: No such file or directory" >&2
    status=1
    continue
  fi

  survivors+=("$src")
done

# One stat for all survivors (same flags as get_device, so a symlink still
# reports the link itself, not its target). Routing between Trash and
# permanent deletion must never ride on misaligned output: if the line count
# doesn't match the input count (a path vanished mid-run), devices is cleared
# and the loop below falls back to one get_device per file.
devices=()
if [[ ${#survivors[@]} -gt 0 ]]; then
  while IFS= read -r line; do
    devices+=("$line")
  done < <(stat -f '%d' -- "${survivors[@]}" 2>/dev/null || true)
  [[ ${#devices[@]} -eq ${#survivors[@]} ]] || devices=()
fi

# Pass 2: route each survivor to Trash or (cross-volume) real rm
i=0
for src in ${survivors[@]+"${survivors[@]}"}; do
  if [[ ${#devices[@]} -gt 0 ]]; then
    src_device="${devices[i]}"
  else
    src_device="$(get_device "$src")"
  fi
  i=$((i + 1))

  # External volume: use real rm (standard Unix behavior)
  if [[ -n "$trash_device" && -n "$src_device" && "$src_device" != "$trash_device" ]]; then
    [[ $verbose -eq 1 ]] && echo "trash: external volume, permanently deleting: $src" >&2
    if [[ -d "$src" && ! -L "$src" ]]; then
      "$REAL_RM" -rf -- "$src" || {
        echo "trash: failed to delete: $src" >&2
        status=1
      }
    else
      "$REAL_RM" -f -- "$src" || {
        echo "trash: failed to delete: $src" >&2
        status=1
      }
    fi
    continue
  fi

  # Same volume: move to Trash
  # Pure-bash basename: $(basename) forks add up across many files
  base="${src%"${src##*[!/]}"}"
  base="${base##*/}"
  dest="$trash_dir/$base"

  # Deconflict: timestamp.PID.counter
  if [[ -e "$dest" || -L "$dest" ]]; then
    [[ -n "$ts" ]] || ts="$(date +%Y%m%d-%H%M%S)"
    counter=$((counter + 1))
    dest="$trash_dir/$base.$ts.$$.$counter"
    # If still exists (unlikely), keep incrementing
    while [[ -e "$dest" || -L "$dest" ]]; do
      counter=$((counter + 1))
      dest="$trash_dir/$base.$ts.$$.$counter"
    done
  fi

  [[ $verbose -eq 1 ]] && printf 'trash: %s -> %s\n' "$src" "$dest" >&2
  command mv -- "$src" "$dest" || {
    echo "trash: failed: $src" >&2
    status=1
  }
done

exit $status
