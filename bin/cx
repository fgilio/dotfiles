#!/usr/bin/env zsh
# Codex wrapper - works in any shell context
# Sandbox config in ~/.codex/config.toml (sandbox_mode = "danger-full-access")

if [ $# -eq 0 ]; then
    # Interactive: xhigh reasoning for deeper exploration
    exec codex --search --dangerously-bypass-approvals-and-sandbox --model gpt-5.4 --config model_reasoning_effort="xhigh"
else
    # Non-interactive: medium reasoning for faster responses
    exec codex --search --dangerously-bypass-approvals-and-sandbox --model gpt-5.4 --config model_reasoning_effort="medium" exec --skip-git-repo-check "$@"
fi
