output:
  - summary
  - failure
  - execution_out

pre-commit:
  commands:
    check:
      run: bin/check
    gitleaks:
      run: gitleaks git --staged --no-banner --redact --config .gitleaks.toml

pre-push:
  commands:
    tests:
      run: bin/rm-test
